apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: kafka-vs
  namespace: kafka
spec:
  hosts:
  - "kafka-service.kafka.svc.cluster.local"
  http:
  - route:
    - destination:
        host: kafka-service.kafka.svc.cluster.local
        port:
          number: 9092
